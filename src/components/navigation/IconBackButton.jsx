import { StyleSheet, Text, View } from "react-native";
import React from "react";

const IconBackButton = () => {
	return (
		<View>
			<Text>IconBackButton</Text>
		</View>
	);
};

export default IconBackButton;

const styles = StyleSheet.create({});
